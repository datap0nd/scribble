# Phase 4 PP01 copy-and-repair candidate

This **synthetic, unapproved candidate** was produced on the Windows workstation with the CI-built `5deacf0` assembly. It is a six-slide editable copy of the deliberately defective PP01 fixture. The original deck and WB01 workbook remained byte-for-byte unchanged. The chart was recreated from WB01 ledger operands; PowerPoint rendered the PPTX to the PDF shown in `contact.png`.

The independent PP01 native-grading projection accepted the candidate: native artifact, presentation, and native chart checks all passed. Its nonblocking measurement review flagged the chart's non-solid/translucent fill for visual review. The complete native repair harness passed its patch stage and PDF package equivalence check. These checks do not certify production integration or visual quality.

The exact hashes, fixture identity, and gate state are in `manifest.json`. `visual_approved=false` remains deliberate. An identified reviewer must approve the exact candidate and the 18 reference fixtures; the reviewer calibration and production repair route also remain open. No paid model call was used.
