# PP01 source-preserving copy candidate

This is the exact disposable six-slide output from the CI-built `e00d279`
native harness. `manifest.json` binds the PPTX, PDF, contact image, source,
workbook, sealed kit, and compiled assembly by SHA-256.

The harness recovered the tagged source-to-draft binding twice, rebuilt the
native chart from the workbook, staged and committed targeted slide repairs,
and checked that the source PPTX and workbook stayed unchanged. The independent
PP01 native grader accepted the candidate's hard artifact, presentation, and
chart checks. Its chart-fill advisory requires visual review.

The model-facing six-slide copy path, human visual approval, and full Scribble
2.0 acceptance remain open. This artifact is a candidate, not a release.
