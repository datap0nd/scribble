# Native acceptance checkpoint — 2026-09-22

Acceptance requires **three consecutive unassisted live passes each** for XA01 (Excel to four-slide deck) and PP01 (repair existing six-slide deck). Each pass must verify factual accuracy, preserved source work, and rendered slide quality. CI and draft creation alone do not count.

| Case | Last live result | Fix / next hypothesis | Consecutive passes |
| --- | --- | --- | --- |
| XA01 | 2.0.385 live run completed unassisted. Native evaluator passed final XLSX/PPTX, source preservation, exact formula-derived May/June values, four-slide structure, chart data, and text fit. Manual inspection of all four rendered slides found a readable scorecard, chart/table comparisons, and structured data-quality cards without clipping or collision. Evidence: `suite-20260922-163550-3fa83055`, `artifacts/qa-385-xa01-run1/`. | Repeat twice without an intervening XA01 failure, with the same source/accuracy/rendered checks. The evaluator records `needs_native_visual_review`, so retain the manual visual assessment alongside native gates. | 1/3 |
| PP01 | 2.0.394 produced a materially improved six-slide deck and passed every deterministic native gate, including exact chart data, six-slide content, text fit, and byte-for-byte preservation of both source files. It does **not** count: the run was paused during visual review, and slide 4 still repeated its KPI values in body copy. The trace also exposed a repair stall where one response addressed two slides. Evidence: `suite-20260922-181316-2a0e40df`, `artifacts/qa-394-pp01-timeout/`. | Filter a multi-slide review into one finding set per repair, allow two bounded pre-mutation schema corrections, and remove promoted KPI values from evidence-card body copy. Next hypothesis: PP01 completes without `SLIDE_REPAIR_COUNT`, keeps 55.76% from verified native arithmetic, and renders six readable slides without duplicated KPI copy. | 0/3 |

At 12:23 UTC, OpenRouter confirmed the user's earlier **$10 top-up** and the active no-reset inference key's total cap was increased from **$20 to $30**. The user reported another $10 purchase after the 2.0.394 run; the active key still last reported a $30 total cap, so the new account balance is not treated as spendable until the key cap is independently verified. The harness checks before each model request and retains a $0.05 reserve. At 18:18 UTC, the key reported **$5.40 remaining**. CI passed for installed 2.0.394. The live acceptance bar remains XA01 1/3 and PP01 0/3. Do not infer passes from CI or a partial native deck. Preserve Office originals and uncertain drafts for inspection; never replay a changed write without receipt reconciliation.
